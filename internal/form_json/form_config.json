[
    {
        "dataKey": "host",
        "label": "设备下行地址",
        "placeholder": "可选，默认使用设备最近一次上报IP",
        "type": "input",
        "validate": {
            "required": false,
            "type": "string"
        }
    },
    {
        "dataKey": "commandUrl",
        "label": "Command 指令推送路径",
        "placeholder": "/api/v1/command",
        "type": "input",
        "default": "/api/v1/command",
        "validate": {
            "required": false,
            "type": "string"
        }
    },
    {
        "dataKey": "controlUrl",
        "label": "Control 控制推送路径",
        "placeholder": "/api/v1/control",
        "type": "input",
        "default": "/api/v1/control",
        "validate": {
            "required": false,
            "type": "string"
        }
    },
    {
        "dataKey": "port",
        "label": "设备下行 HTTP 端口",
        "placeholder": "8080",
        "type": "input",
        "default": "8080",
        "validate": {
            "required": false,
            "type": "string"
        }
    }
]
